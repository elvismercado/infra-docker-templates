# WUD (What's Up Docker?) labels - notify + auto-update the floating latest digest

services:
  syncthing:
    labels:
      - "wud.watch=true"
      - "wud.watch.digest=true"
      - "wud.tag.include=^latest$$"
      - "wud.trigger.include=smtp.gmail,docker.local"
