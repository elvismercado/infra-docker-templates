# WUD (What's Up Docker?) labels - notify + auto-update the floating LTS digest

services:
  portainer:
    labels:
      - "wud.watch=true"
      - "wud.watch.digest=true"
      - "wud.tag.include=^lts$$"
      - "wud.trigger.include=smtp.gmail,docker.local"
